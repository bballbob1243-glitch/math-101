const q=new URLSearchParams(location.search),name=q.get('name')||'Player',skin=q.get('skin')||'blue';const socket=io();let me=null,others=new Map();socket.on('connect',()=>socket.emit('join',{name,skin,mode:location.pathname.includes('horror')?'horror':'battle'}));socket.on('joined',p=>{me=p});socket.on('players',ps=>ps.forEach(p=>{if(p.id!==me?.id)others.set(p.id,p)}));socket.on('state',p=>{if(p.id!==me?.id)others.set(p.id,p)});socket.on('left',id=>others.delete(id));socket.on('full',()=>alert('The server is full. Maximum 15 players.'));
const keys={};addEventListener('keydown',e=>keys[e.key.toLowerCase()]=true);addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);let x=0,z=0;setInterval(()=>{if(!me)return;const sp=.18;if(keys.w||keys.arrowup)z-=sp;if(keys.s||keys.arrowdown)z+=sp;if(keys.a||keys.arrowleft)x-=sp;if(keys.d||keys.arrowright)x+=sp;me.x=x;me.z=z;socket.emit('state',{x,z,y:1,hp:me.hp,mode:me.mode})},50);
